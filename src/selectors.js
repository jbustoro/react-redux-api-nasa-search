export const searchTermSelector = state => state.searchTerm;
export const resultsSelector = state => state.results;
export const fetchStateSelector = state => state.fetchState;
export const errorMessageSelector = state => state.errorMessage;
